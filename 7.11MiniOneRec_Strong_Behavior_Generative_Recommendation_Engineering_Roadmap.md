# MiniOneRec Strong Behavior Generative Recommendation 工程实施总纲

> 版本：v2.0  
> 日期：2026-07-10  
> 项目：MiniOneRec / CS-SID / Dual-SID / Strong Behavior-SID  
> 目标：在现有 MiniOneRec 复现、CS-SID 和 Dual-SID 优化基础上，形成一条可复现、可消融、可用于推荐算法实习面试的生成式推荐优化主线。

---

## 0. 文档定位

本文档是后续本地 Codex 开发、AutoDL 实验和项目总结的统一工程依据，解决四个问题：

1. 当前已经完成了什么，哪些结果已冻结；
2. 本地 WSL、Git、VSCode Codex 与 AutoDL 如何分工；
3. 后续每个阶段修改什么、使用什么工具、生成什么产物；
4. 每个阶段通过什么指标验收，何时继续，何时停止。

本文档不要求一次性实现全部阶段。必须按照阶段门推进：上一阶段的工程契约和 validation 验收通过后，才能进入下一阶段。

---

## 1. 固定开发与实验工作流

### 1.1 唯一代码真源

本项目固定采用：

```text
本地 Windows + WSL Ubuntu 24.04
    └── VSCode Remote WSL
        └── 本地 MiniOneRec Git 仓库
            └── Codex 读取、审计、修改代码
```

本地 WSL Git 仓库是唯一代码真源：

```text
repo: 1767822875cyt0801-maker/MiniOneRec.git
branch: yuting-reproduce
```

所有代码修改、diff 审查、commit、branch 管理和 GitHub 同步都在本地完成。

### 1.2 AutoDL 的角色

AutoDL 只承担：

```text
GPU 训练
GPU 推理
beam search evaluate
大规模候选生成
需要 GPU checkpoint 的正式实验
```

AutoDL 不作为代码真源，不在 AutoDL 上进行正式 Git 同步，不依赖 AutoDL 仓库状态保存开发历史。

### 1.3 手动双向同步

代码方向：

```text
本地 Codex 修改并检查代码
        ↓
本地 Git diff / commit
        ↓
手动把本轮修改文件同步到 AutoDL
        ↓
AutoDL 执行命令
```

结果方向：

```text
AutoDL 生成日志、指标、manifest、必要 checkpoint 摘要
        ↓
手动复制回本地仓库或本地 stage_outputs
        ↓
本地 Codex 读取结果并判断下一步
```

每轮同步必须生成或人工记录 changed-file manifest，至少包括：

```text
相对路径
文件用途
是否需要覆盖 AutoDL 旧文件
AutoDL 运行入口
预期输出目录
```

### 1.4 本地 Codex 的允许与禁止事项

允许：

- 读取本地仓库和技术文档；
- 修改代码、脚本、测试和文档；
- 运行静态检查、单元测试和小型 CPU smoke；
- 生成 AutoDL 命令行；
- 检查 Git diff，但不擅自清理用户已有改动。

禁止：

- 在本地启动正式 GPU 训练；
- 为了“快速验证”使用 test 调参；
- 擅自修改或删除冻结结果；
- 擅自重写无关文件；
- 在没有证据时声称某个阶段已经通过；
- 一次性实现所有后续研究扩展。

---

## 2. 当前项目状态与冻结结果

### 2.1 已完成的主线

```text
Stage 0  SID 契约、manifest 和映射检查
Stage 1  SID 静态诊断与 calc_plus 增强评测
Stage 2  train-only PPMI/SVD CF embedding 与 CS embedding
Stage 3  RQ-KMeans / MiniBatchKMeans SID 构造与 append dedup
Stage 4  train/valid/test CSV 重写与 SID 契约检查
Stage 5  sid-only SFT 消融
Stage 6  candidate expansion 与 history-aware rerank
Stage 6.5 Text-SID + CF-SID Dual-SID item-level fusion
Stage 7  validation protocol 与 learned ranker 筛选
```

### 2.2 当前关键代码

```text
build_cs_embeddings.py
run_rqkmeans_with_emb.py
rewrite_sid_csv.py
update_sid_manifest.py
check_sid_stage0.py
analyze_sid.py
calc_plus.py
evaluate.py
evaluate_candidates.py
fuse_dual_sid_candidates.py
rerank.py
```

### 2.3 当前 embedding 与 SID

现有 behavior embedding：

```text
train interaction
-> item co-occurrence
-> PPMI
-> TruncatedSVD
-> cf_emb.npy
```

它已经证明能够提供 Text-SID 缺失的互补候选，但不属于强序列行为模型。

现有主要 SID：

```text
text_mbk_k512_dedup
cf_k512_dedup
cs_alpha0.2_k512_dedup
cs_alpha0.5_k512_dedup
cs_alpha0.7_k512_dedup
```

### 2.4 冻结 test 结果

#### Industrial_and_Scientific

```text
Dual-SID exact + rerank
HR@20   = 0.145158
NDCG@20 = 0.080448

旧 Stage 6 CS best
HR@20   ≈ 0.1366
NDCG@20 ≈ 0.0784
```

#### Office_Products

```text
Text exact HR@20  = 0.144472
CF exact HR@20    = 0.142622
Union HR@20       = 0.189684
RRF HR@20         = 0.162145
Rerank HR@20      = 0.172215
Rerank NDCG@20    = 0.101124
```

这些 test 数值只能作为冻结 baseline，不得用于后续超参数选择。

### 2.5 Stage 7 当前状态

```text
P1 validation protocol：通过
P2-2 minimal learned ranker：do_not_freeze
P2-3 history-aware ranker：freeze
P2-4 closeout：通过
P3：待在 frozen history ranker 下完成 validation fusion sweep
```

下一步不是重新做 learned ranker，而是先完成 P3 validation 选参，再进入 Strong Behavior-SID。

---

## 3. 总研究问题

后续项目围绕五个研究问题组织：

1. PPMI/SVD 能否被 SASRec 强行为 embedding 替代，并生成更好的 Behavior-SID？
2. semantic 和 behavior 应该在 embedding、token-level training 还是 item candidate 层融合？
3. token cross-entropy 是否与 beam-search 下的 HR/NDCG 对齐？
4. exact、prefix expansion 和 constrained decoding 如何平衡合法性、召回率和噪声？
5. 当前 text encoder 和 item metadata 是否限制了 Text-SID 上限？

研究扩展问题：

6. 真实多行为日志下，view/click/cart/buy/dwell 应如何进入生成式推荐？
7. 是否需要 context-aware token representation 或一个 item 的少量 contextual SID prototype？

---

## 4. 总体阶段主线

| 阶段 | 名称 | 级别 | 当前状态 |
|---|---|---|---|
| S0 | Validation closeout | 必须 | 进行中 |
| S1 | Data/Feedback Audit | 必须 | 待做 |
| S2 | SASRec Strong Behavior Model | 必须 | 待做 |
| S3 | Strong Behavior-SID | 必须 | 待做 |
| S4 | Fusion-location Ablation | 项目核心 | 待做 |
| S5 | Beam-aware Objective Alignment | 强烈建议 | 待做 |
| S6 | Candidate Generation Optimization | 必须 | 待做 |
| S7 | Strong Semantic Encoder Audit | 强烈建议 | 待做 |
| S8 | Multi-behavior Dataset Case Study | 可选 | 不阻塞主线 |
| S9 | Context-aware Tokenization | 高风险扩展 | 不阻塞主线 |

---

## 5. S0：Validation closeout

### 5.1 目标

在 frozen P2 history ranker 下，只使用 valid 选择 RRF/fusion 参数，然后执行一次最终 test confirmation。

### 5.2 工具

```text
fuse_dual_sid_candidates.py
rerank.py
freeze_stage7_p2_history_ranker.py
run_stage7_p2_frozen_ranker_closeout.sh
待检查/补齐的 P3 validation sweep runner
```

### 5.3 选择协议

预先固定：

```text
主指标：valid NDCG@20
次指标：valid HR@20
再次相同：candidate 更少者优先
仍相同：选择更简单、更接近默认值的参数
```

建议分阶段搜索 lambda 与 source_weight，不进行无边界联合搜索。

### 5.4 产物

```text
results/stage7_validation_protocol/fusion_sweep_valid.csv
results/stage7_validation_protocol/selected_config.json
results/stage7_validation_protocol/final_test_confirmation.json
results/stage7_validation_protocol/protocol_closeout.md
```

### 5.5 验收

- 所有 sweep 输入为 valid；
- history ranker 文件和特征定义保持冻结；
- test 只执行一次；
- selected config 可追溯；
- 不根据 final test 返回调参。

---

## 6. S1：Data/Feedback Audit

### 6.1 目标

明确 Amazon 原始数据与当前 CSV 中的反馈字段，判断 rating 是否被丢弃、低评分事件是否被当作正向 next-item target，并形成数据卡。

### 6.2 工具

建议新增：

```text
tools/audit_amazon_feedback_semantics.py
tests/test_audit_amazon_feedback_semantics.py
```

检查：

```text
raw field inventory
rating distribution
timestamp coverage
current CSV columns
train/valid/test chronological split
target rating distribution（若可恢复）
history truncation
user/item 5-core filtering
```

### 6.3 产物

```text
results/data_feedback_audit/<category>/raw_field_inventory.json
results/data_feedback_audit/<category>/rating_distribution.csv
results/data_feedback_audit/<category>/interaction_semantics.json
docs/data_feedback_audit.md
```

### 6.4 验收

- 明确当前 interaction 的语义；
- 明确 rating 是否进入模型；
- 明确不能将 Amazon 实验描述成 click/cart/purchase 建模；
- train/valid/test 时间边界无泄漏；
- 审计脚本在字段缺失时给出明确结论，而不是崩溃或推测。

---

## 7. S2：SASRec Strong Behavior Model

### 7.1 目标

只使用 official train 构造序列训练 SASRec，valid 仅用于 checkpoint selection，验证其 item-ID 推荐性能后导出全局 item embedding。

### 7.2 优先复用

Codex 必须先搜索仓库是否已有：

```text
SASRec model
traditional baseline implementation
item index mapping
sequence dataloader
evaluation functions
```

只有确认不可复用时，才新增最小 PyTorch SASRec 实现。

### 7.3 推荐第一版配置

```text
max_seq_len = 10
embedding_dim = 128 或 256
num_layers = 2
num_heads = 2 或 4
dropout = 0.2
loss = full-softmax cross entropy
checkpoint metric = valid NDCG@20
```

当前 catalog 约 3500 个 item，可优先使用 full softmax，避免负采样引入额外变量。

### 7.4 建议新增

```text
models/sasrec.py
build_sasrec_embeddings.py
scripts/run_sasrec_embedding_smoke.sh
tests/test_sasrec_data_contract.py
tests/test_sasrec_embedding_export.py
```

实际文件位置应遵循仓库现有结构，不能为了匹配本文档强行创建重复目录。

### 7.5 导出契约

```text
data/Amazon/behavior_embeddings/sasrec/<category>/
  sasrec_item_emb.npy
  row_index.json
  item_order.json
  train_item_count.json
  cold_item_report.json
  config.yaml
  checkpoint metadata
```

### 7.6 cold item

所有 train count 为 0 的 item 必须：

- 被统计；
- 使用明确 fallback；
- 不得产生 NaN/全零未解释行；
- 不得使用 valid/test interaction 学习 fallback。

### 7.7 验收

- train-only 学习，valid-only checkpoint selection；
- item mapping 与现有 SID item_order 100% 一致；
- embedding 无 NaN/Inf；
- SASRec valid HR/NDCG 高于 popularity 和轻量 behavior baseline；
- 只有 SASRec 本身通过后才进入 S3。

---

## 8. S3：Strong Behavior-SID

### 8.1 目标

使用 SASRec item embedding 生成 SASRec-SID，并与 PPMI CF-SID、Text-SID 公平比较。

### 8.2 工具

复用：

```text
run_rqkmeans_with_emb.py
rewrite_sid_csv.py
update_sid_manifest.py
check_sid_stage0.py
analyze_sid.py
scripts/run_sid_30k_validation.sh
evaluate_candidates.py
```

### 8.3 默认配置

```text
codebook_size = 512
num_levels = 3
dedup_mode = append
seed 固定
第一轮 candidate mode = exact
```

### 8.4 静态指标

```text
pre/post dedup collision
d-token ratio
prefix bucket size distribution
per-level code utilization
entropy / Gini
behavior similarity within prefix
transition overlap within prefix
```

### 8.5 动态评测

```text
5k/10k SFT smoke
通过后 30k SFT
beam20 exact
valid HR/NDCG
behavior-only hits
Text+SASRec union oracle recall
```

### 8.6 验收

- post-dedup collision = 0；
- item2sid/sid2items/CSV 全部通过契约；
- SASRec-SID 至少在 valid 推荐性能、d-token ratio、前缀行为一致性或 union complementarity 中优于 PPMI CF-SID；
- 单流不必强制超过 Text-SID，但必须证明强 behavior source 有独立价值。

---

## 9. S4：Fusion-location Ablation

### 9.1 核心研究问题

比较 semantic 与 behavior 的三种融合位置：

```text
Early：embedding -> CS-SID
Middle：token-level collaborative alignment
Late：two-stream item candidate fusion
```

三组必须使用同一个 SASRec behavior source。

### 9.2 Early

```text
normalize/project text embedding
normalize/project SASRec embedding
alpha fusion
RQ-KMeans -> Strong CS-SID
```

只在 valid 检查少量 alpha，例如 0.2/0.5/0.8。

### 9.3 Middle

优先实现 TCA4Rec-lite：

```text
SASRec item-level logits
-> 按合法 SID prefix 聚合为 token distribution
-> 与 one-hot target 混合
-> soft next-token prediction loss
```

形式：

```text
L = (1-lambda) * CE + lambda * KL(q_cf || p_qwen)
```

第一版不修改 Qwen 主体架构，不直接复刻 EAGER 双 decoder。

### 9.4 Late

```text
Text-SID model
SASRec-SID model
-> exact candidates
-> union/RRF
-> frozen history-aware ranker
```

### 9.5 验收

- 三种融合使用相同数据、模型预算、beam 和 behavior source；
- 能分离 SID、训练目标和 ranker 的贡献；
- 至少一种 Strong Behavior 融合在 valid 上超过 Text-only 和旧 PPMI Dual-SID；
- 即使 middle 未胜出，也必须形成可解释消融结论。

---

## 10. S5：Beam-aware Objective Alignment

### 10.1 目标

解决完整 target SID 概率较高，但正确 prefix 在 beam search 中被提前剪掉的问题。

### 10.2 第一版

优先实现 BEAR-lite：

```text
对每个 SID level：
  取合法 next-token logits
  计算正 token 分数
  计算 beam width 对应的第 B 大分数
  对正 token 未进入 Top-B 的情况施加 margin/softplus loss
```

### 10.3 对照

```text
CE
CE + BEAR-lite
CE + existing GRPO/HEPO
CE + BEAR-lite + GRPO/HEPO
```

### 10.4 指标

```text
target token rank per level
target prefix survival per level
premature pruning rate
beam10/20/50 HR/NDCG
invalid SID rate
training overhead
```

### 10.5 验收

- prefix survival 与最终 HR/NDCG 同方向改善；
- loss 只对合法 SID next-token 计算；
- 不增加 invalid SID；
- selected 正式配置至少跑 3 seeds。

---

## 11. S6：Candidate Generation Optimization

### 11.1 对照

```text
exact constrained decoding
ungated p3
bucket-threshold gated p3
top-M p3
dual-agreement p3
confidence-gated p3
```

### 11.2 指标

```text
valid_sid_rate
exact/prefix candidate recall
avg/p95 candidate count
union oracle HR
final rerank HR/NDCG
candidate-to-hit conversion
latency/query
```

### 11.3 验收

- exact valid SID rate = 100%；
- gated p3 候选数低于 ungated p3；
- candidate budget 不超过预设上限，例如 500；
- final NDCG/HR 不低于 exact；
- gate 参数只在 valid 上选择。

---

## 12. S7：Strong Semantic Encoder Audit

### 12.1 变量分离

Metadata：

```text
title
title + category
title + category + brand
title + category + brand + description
```

Encoder：

```text
current encoder
Qwen3-Embedding-0.6B
optional BGE-M3
```

### 12.2 公平性

```text
相同 item set
相同 normalization
统一 projection/PCA dimension
相同 codebook/depth/dedup
相同 SFT budget
```

### 12.3 筛选

```text
静态 SID quality
-> 10k SFT smoke
-> 只对 valid winner 做 30k 正式实验
```

### 12.4 验收

- 至少在 tail/cold、SID quality 或 valid HR/NDCG 上产生明确收益；
- 如果通用 embedding 更强但推荐指标未提升，应保留负结论，不进行 test 追参。

---

## 13. S8：Multi-behavior Case Study（可选）

### 13.1 数据选择

```text
工程优先：RetailRocket（view/cart/transaction）
研究优先：KuaiRand（随机曝光 + 多类反馈）
```

Amazon 主线保留，不直接替换。

### 13.2 表示

```text
<view> <item_sid...>
<like> <item_sid...>
<purchase> <item_sid...>
```

行为 token 与 item SID 分离。

### 13.3 任务

```text
given behavior -> generate item
history -> generate next behavior -> generate next item
```

### 13.4 验收

- 使用真实行为标签，不把 rating 伪装成 click/cart；
- 分行为报告 HR/NDCG/F1；
- 重点评价稀疏高价值行为；
- 如果有曝光日志，明确处理曝光偏差。

---

## 14. S9：Context-aware Tokenization（高风险扩展）

优先级：

```text
DECOR-lite contextual token embedding
-> Pctx-light 少量 item context prototype
-> ActionPiece-light feature-set tokenizer
```

禁止直接构造 `(user_id, item_id) -> unique SID`。

验收：

```text
平均 prototype 数可控
多 SID 可确定 grounding 回同一 item
无 valid/test context 泄漏
长历史/多意图用户有稳定收益
trie 与 serving 成本可接受
```

---

## 15. 统一实验治理规则

### 15.1 数据泄漏

```text
train：训练 embedding/model/statistics
valid：checkpoint、超参数和 gate 选择
test：阶段最终一次确认
```

Popularity、co-occurrence、SASRec、rerank 特征统计都只能来自 train。

### 15.2 公平对照

每个比较必须固定：

```text
dataset rows
Qwen backbone
sample count
epoch
learning rate
beam width
candidate budget
evaluation code
seed policy
```

### 15.3 两级验收

每个阶段分为：

```text
Engineering pass：契约、映射、脚本、可复现性通过
Research pass：valid 指标或诊断指标支持研究假设
```

工程未通过不得解释性能；研究未通过不得继续全量扩展。

### 15.4 Seeds

```text
smoke：1 seed
validation screening：1 seed
selected final configuration：至少 3 seeds
```

### 15.5 结果目录最低契约

每次正式实验必须包含：

```text
config.yaml/json
run.log
artifact_manifest.json
metrics_summary.json/csv
metrics_summary.md
consistency_report.json
```

---

## 16. 本地到 AutoDL 的每轮操作清单

### 16.1 本地修改前

```bash
cd ~/projects/MiniOneRec
git status
git branch --show-current
git diff --stat
```

记录用户已有改动，Codex 不得覆盖无关文件。

### 16.2 Codex 修改后

```bash
git diff --check
git status --short
git diff --stat
```

运行本地允许的：

```text
静态检查
单元测试
CPU tiny smoke
shell DRY_RUN
```

### 16.3 形成同步清单

```bash
git diff --name-only
git diff --name-status
```

如已 commit，可使用：

```bash
git diff --name-status <previous_commit>..HEAD
```

### 16.4 AutoDL 覆盖前

在 AutoDL 项目目录确认：

```bash
cd /root/autodl-tmp/projects/MiniOneRec
pwd
```

只同步 manifest 中的文件。不要整体覆盖 `data/`、`results/`、checkpoint 或环境文件。

### 16.5 AutoDL 执行

顺序：

```text
环境检查
DRY_RUN
小规模 smoke
检查产物
正式 GPU run
```

### 16.6 结果回传

优先回传：

```text
run.log
config
metrics
consistency report
artifact manifest
必要的 per-sample diagnostics
```

大型 checkpoint 只记录路径、哈希和配置，除非本地确实需要读取。

---

## 17. 工具—阶段映射

| 工具/脚本 | 主要阶段 | 用途 |
|---|---|---|
| `build_cs_embeddings.py` | 历史基线 | PPMI/SVD CF/CS embedding |
| `build_sasrec_embeddings.py` | S2 | SASRec 训练与 embedding 导出 |
| `run_rqkmeans_with_emb.py` | S3/S7 | embedding 到 SID |
| `rewrite_sid_csv.py` | S3 | 重写 SID CSV |
| `check_sid_stage0.py` | S3 | SID 契约检查 |
| `analyze_sid.py` | S3/S7 | collision/prefix/code quality |
| `evaluate.py` | S3–S7 | beam prediction |
| `evaluate_candidates.py` | S3/S6 | exact/prefix item candidates |
| `fuse_dual_sid_candidates.py` | S0/S4/S6 | item-level fusion |
| `rerank.py` | S0/S4/S6 | frozen rerank |
| `calc_plus.py` | 全阶段 | HR/NDCG 与分桶诊断 |

`build_sasrec_embeddings.py` 等为建议名称，实际实现应优先遵循现有仓库结构。

---

## 18. 面试项目最终叙事

建议项目标题：

> Behavior-Semantic Generative Recommendation with Multi-stage Fusion and Beam-aware Alignment

核心贡献：

1. 将弱 PPMI/SVD 行为表示升级为 train-only SASRec，并构造 Strong Behavior-SID；
2. 在相同 behavior source 下系统比较 early、middle、late 三种 behavior-semantic 融合位置；
3. 建立 validation-only 选参与 query-level split，避免 test tuning；
4. 从 prefix survival 角度诊断并优化 SFT 与 beam search 的训练—推理错位；
5. 用 constrained exact、gated prefix expansion 和 frozen ranker分离合法性、候选召回和排序损失。

边界声明：

> Amazon 结果属于公开 single-event sequential benchmark，不等价于工业 click/cart/purchase 全链路效果；真实多行为和曝光偏差作为独立扩展验证。

---

## 19. 权威工作对应关系

| 工作 | 对本项目的启发 |
|---|---|
| SASRec | 强序列行为 embedding 与 item-ID baseline |
| TIGER | semantic embedding -> RQ tokenizer -> SID generation |
| EAGER | behavior/semantic 双流及置信度融合 |
| LETTER | semantic、collaborative、code diversity 和 ranking-guided loss |
| COSETTE/MARIUS | recommendation-aligned collaborative tokenization 与强 SASRec baseline |
| DiscRec | semantic/collaborative 解耦和模型内 gate |
| UNGER | unified code 与跨模态联合优化 |
| ETEGRec | tokenizer 与 recommender 端到端对齐 |
| TCA4Rec | item-level CF 到 token-level soft supervision |
| BEAR/APAO | beam-search-aware prefix optimization |
| OneRec | retrieve/rank 统一与 preference alignment |
| SETRec | order-agnostic identifier 与 simultaneous grounding |
| ActionPiece | context-aware action feature-set tokenization |
| Pctx/DECOR | personalized/contextual tokenization 和表示 |
| HSTU | 工业 heterogeneous action sequence 与 retrieval/ranking 任务定义 |

---

## 20. 当前立即执行的第一批任务

第一批只做：

```text
A. 审计并补齐 S0/P3 validation fusion sweep
B. 新增 S1 data/feedback audit
C. 审计现有 SASRec 代码，并搭建 S2 train-only SASRec embedding builder
D. 提供本地 CPU smoke 与 AutoDL GPU 命令
```

本批不做：

```text
不构造 SASRec-SID 正式版本
不启动 30k Qwen SFT
不实现 TCA4Rec/BEAR/APAO
不做 p3 新调参
不切换数据集
不运行 test tuning
```

本批完成并由 AutoDL 返回 valid/embedding 证据后，再决定是否进入 S3。

