# MiniOneRec 下一阶段优化最终方案：Validation Protocol + Strong Behavior-SID + Source-aware Rerank

> 版本：Final Plan v1  
> 日期：2026-07-03  
> 项目：MiniOneRec / CF-SID / Dual-SID / Strong Behavior-SID  
> 目标：在当前 Text-SID、CS-SID、CF-SID、Dual-SID 已完成闭环的基础上，继续推进更干净、更强、更可解释的下一阶段优化。

---

## 0. 最终判断

Codex 对上一版方案的评价是合理的，尤其指出了几个必须修正的工程点：

1. **方案方向可行，但不能按原文直接执行。**  
   当前 MiniOneRec 已经具备 `item embedding -> SID -> rewrite CSV -> SFT -> evaluate candidates -> fusion/rerank` 的后半段闭环，但还缺少干净的 `valid split` 支持和 `Strong Behavior Embedding exporter`。

2. **下一阶段最缺的不是后半段 SID pipeline，而是干净的 train-only behavior embedding builder。**  
   当前 `cf_emb.npy` 是 PPMI/SVD 共现向量，已经证明有互补价值，但不是强行为模型。下一步应该优先整理 SASRec embedding exporter，再考虑 LightGCN。

3. **不能让 learned rerank 掩盖 Strong Behavior-SID 的贡献。**  
   后续所有 behavior SID 都必须同时报告：
   - RRF
   - current heuristic rerank
   - learned rerank

   否则如果结果提升，很难判断是 SID 更好，还是 reranker 更强。

4. **validation protocol 是第一优先级。**  
   当前 `run_dual_sid_cf_fusion.sh`、`eval_smoke_sid_version.sh` 等脚本偏 test pipeline，必须先支持：
   - `EVAL_SPLIT=valid/test`
   - `EVAL_CSV=manifest["valid_csv"] 或 manifest["test_csv"]`
   - 输出目录带 split 名称

5. **learned rerank 的训练/dev 切分必须按 sample/row 切，而不能按 candidate 行随机切。**  
   一个 row 下的几百个 candidates 共享同一个 target/history，如果 candidate 行随机切，会产生 query-level 泄漏。

因此最终路线调整为：

```text
P0: 结果归档与基线冻结
P1: Validation protocol 改造
P2: Source-aware learned rerank，但保留 RRF / heuristic 对照
P3: SASRec embedding builder smoke
P4: SASRec-SID quality + 小规模 SFT + dual fusion
P5: LightGCN embedding builder + LightGCN-SID
P6: Gated p3 / noise-aware prefix expansion
P7: ActionPiece-light / learnable tokenizer，作为可选研究增强
```

---

## 1. 当前项目已有基础

### 1.1 已完成的主线

当前 MiniOneRec 已经完成了以下阶段：

```text
Stage 0: SID 契约与 manifest
Stage 1: SID 静态诊断与 calc_plus 增强评估
Stage 2: train-only CF embedding 与 CS embedding
Stage 3/4: RQ-KMeans / MiniBatchKMeans 生成新 SID，append dedup，重写 CSV
Stage 5: sid-only SFT 消融
Stage 6: candidate expansion + lightweight rerank
Stage 6.5: Dual-SID Text+CF Candidate Fusion Extension
Stage 8: Completion audit / final summary
```

已有关键脚本：

```text
build_cs_embeddings.py
run_rqkmeans_with_emb.py
rewrite_sid_csv.py
update_sid_manifest.py
check_sid_stage0.py
analyze_sid.py
calc_plus.py
evaluate.py
evaluate_candidates.py
fuse_dual_sid_candidates.py
rerank.py
summarize_final_results.py
```

其中：

```text
evaluate_candidates.py:
    将 SID beam 展开为 item candidates，支持 exact 和 prefix expansion。

fuse_dual_sid_candidates.py:
    将 Text-SID 与 behavior/CF-SID 两路 candidates 做 item-level fusion。

rerank.py:
    非泄漏轻量 rerank，使用 SID rank、source、train-only popularity、history/recent cosine、bucket penalty。

run_rqkmeans_with_emb.py:
    从任意 item embedding matrix 生成 SID，支持 canonical sid_versions 目录和 append dedup。
```

---

### 1.2 当前模型设置

当前 Text-SID、CS-SID、CF-SID stream 使用同一个 Qwen 底座：

```text
model_path = /root/autodl-tmp/models/Qwen2.5-0.5B
model_type = qwen2
num_hidden_layers = 24
hidden_size = 896
intermediate_size = 4864
num_attention_heads = 14
num_key_value_heads = 2
vocab_size = 151936
max_position_embeddings = 32768
```

这说明：

```text
Text stream / CS stream / CF stream 的模型架构相同。
差异只在 SID version、tokenizer 扩展 token、train/valid/test CSV 中的 history_item_sid 和 item_sid。
```

---

### 1.3 当前 embedding 基础

已有 CF/CS embedding：

```text
data/Amazon/cs_embeddings/Industrial_and_Scientific/
  Industrial_and_Scientific.cf_emb.npy
  Industrial_and_Scientific.cs_emb_alpha0.2.npy
  Industrial_and_Scientific.cs_emb_alpha0.5.npy
  Industrial_and_Scientific.cs_emb_alpha0.7.npy
  Industrial_and_Scientific.row_index.json
  Industrial_and_Scientific.item_order.json

data/Amazon/cs_embeddings/Office_Products/
  Office_Products.cf_emb.npy
  Office_Products.cs_emb_alpha0.2.npy
  Office_Products.cs_emb_alpha0.5.npy
  Office_Products.cs_emb_alpha0.7.npy
  Office_Products.row_index.json
  Office_Products.item_order.json
```

当前 CF embedding 形状：

| Category | `cf_emb.npy` shape | zero rows | 含义 |
|---|---:|---:|---|
| Industrial_and_Scientific | `(3686, 2560)` | 40 | train-only PPMI/SVD item CF 向量 |
| Office_Products | `(3459, 2560)` | 14 | train-only PPMI/SVD item CF 向量 |

解释：

```text
cf_emb.npy 是轻量行为 embedding，不是 SASRec / LightGCN。
它来自 train-only item co-occurrence -> PPMI -> SVD。
它已经证明能提供 Text-SID 漏掉的候选，但仍可能弱于强行为模型。
```

---

### 1.4 当前 SID version 基础

当前已有主要 SID version：

```text
text_mbk_k512_dedup
cf_k512_dedup
cs_alpha0.2_k512_dedup
cs_alpha0.5_k512_dedup
cs_alpha0.7_k512_dedup
```

其中 `cf_k512_dedup` 的质量：

| Category | num_items | unique_sids | collision_sids | max_bucket | sid_len_dist |
|---|---:|---:|---:|---:|---|
| Industrial_and_Scientific | 3686 | 3686 | 0 | 1 | `{3:1504, 4:2182}` |
| Office_Products | 3459 | 3459 | 0 | 1 | `{3:1536, 4:1923}` |

解释：

```text
post-dedup 后 CF-SID 完全唯一，工程契约 OK。
但是 4 层 SID 占比较高，说明前三层聚类可分性有限，CF-SID 更依赖 <d_*> dedup。
```

Text-SID 对照：

| Category | sid_len_dist |
|---|---|
| Industrial text_mbk_k512_dedup | `{3:3129, 4:557}` |
| Office text_mbk_k512_dedup | `{3:3031, 4:428}` |

说明：

```text
CF-SID 对 <d_*> 的依赖明显高于 Text-SID。
下一阶段 Strong Behavior-SID 需要观察是否能降低 d-token ratio。
```

---

### 1.5 当前 Dual-SID 结果

#### Industrial_and_Scientific

| Mode | Text HR@20 | CF HR@20 | Union HR@20 | Behavior-only hits@20 | RRF HR@20 | Rerank HR@20 | Rerank NDCG@20 |
|---|---:|---:|---:|---:|---:|---:|---:|
| exact | 0.128612 | 0.120450 | 0.163468 | 158 | 0.130157 | 0.145158 | 0.080448 |
| p3 | 0.112508 | 0.073902 | 0.138760 | 119 | 0.113832 | 0.141407 | 0.079782 |

#### Office_Products

| Mode | Text HR@20 | CF HR@20 | Union HR@20 | Behavior-only hits@20 | RRF HR@20 | Rerank HR@20 | Rerank NDCG@20 |
|---|---:|---:|---:|---:|---:|---:|---:|
| exact | 0.144472 | 0.142622 | 0.189684 | 220 | 0.162145 | 0.172215 | 0.101124 |
| p3 | 0.141800 | 0.113440 | 0.177148 | 172 | 0.148788 | 0.168722 | 0.099451 |

结论：

```text
CF-only 不是稳定强于 Text-only。
但 behavior-only hits 明显存在，说明 CF-SID 能补充 Text-SID 漏掉的候选。
Dual-SID exact + rerank 是当前最强 item-level result。
```

与 Stage 6 CS-SID 对比：

| Category | Stage 6 CS best | Dual exact + rerank | 增益 |
|---|---:|---:|---:|
| Industrial HR@20 | 0.1366 | 0.145158 | +0.008558 |
| Industrial NDCG@20 | 0.0784 | 0.080448 | +0.002048 |
| Office HR@20 | 0.1492 | 0.172215 | +0.023015 |
| Office NDCG@20 | 0.0884 | 0.101124 | +0.012724 |

因此：

```text
CS-SID = strongest single-SID mainline。
Dual-SID exact + rerank = strongest item-level extension。
```

---

## 2. 当前不足和下一阶段目标

### 2.1 当前不足

| 编号 | 不足 | 后续解决模块 |
|---|---|---|
| 1 | 当前 CF embedding 是 PPMI/SVD，不是 SASRec/LightGCN 强行为模型 | SASRec-SID / LightGCN-SID |
| 2 | CF-SID 对 `<d_*>` dedup 依赖较大，前三层聚类可分性有限 | Strong Behavior-SID + SID quality analysis |
| 3 | RRF 对 lambda 权重敏感 | Validation-calibrated learned rerank |
| 4 | rerank 未吃满 union recall，有约 1.7-1.8 HR 点排序损失 | Source-aware learned rerank |
| 5 | p3 在 dual-stream 下噪声偏大 | Gated p3 / Top-M p3 / Agreement-p3 |
| 6 | 后续调参必须用 validation，不能继续 test-set tuning | Validation protocol 改造 |

---

### 2.2 下一阶段总目标

最终目标不是简单堆模块，而是形成一个干净的研究链：

```text
先建立 validation protocol，
再验证更强排序器能否吃掉 union recall，
再验证强行为 embedding 是否优于 PPMI/SVD，
最后再做 prefix noise control 和 tokenizer 级增强。
```

具体目标：

```text
1. 所有 fusion/rerank 超参只在 validation 上选择。
2. learned rerank 必须按 row/query 切分，避免 candidate-level 泄漏。
3. SASRec builder 必须导出明确的 item embedding。
4. LightGCN 需要新增实现，不能假设项目已有。
5. 每个 behavior SID 都必须同时报告 RRF / heuristic rerank / learned rerank。
6. 先 exact，再 p3；不要一开始被 p3 噪声干扰。
```

---

## 3. 总体工程目录设计

建议新增：

```text
results/stage7_validation_protocol/
results/stage7_5_learned_rerank/
results/stage8_sasrec_behavior_sid/
results/stage8_5_lightgcn_behavior_sid/
results/stage9_gated_p3/
results/stage10_actionpiece_light/
```

建议新增：

```text
data/Amazon/behavior_embeddings/sasrec/
data/Amazon/behavior_embeddings/lightgcn/
```

每个阶段都必须输出：

```text
config.yaml
run.log
artifact_manifest.json
metrics_summary.csv
metrics_summary.md
consistency_report.json
```

---

## 4. P0：基线归档与冻结

### 4.1 目标

在开始下一轮之前，把当前可用的结果固定下来，作为不可再调参的 baseline。

### 4.2 输入

```text
results/final_summary/final_results_summary.md
results/final_summary/final_consistency_report.md
results/dual_sid_cf_k512_dedup_Industrial_and_Scientific/
results/dual_sid_cf_k512_dedup_Office_Products/
data/Amazon/sid_versions/text_mbk_k512_dedup/
data/Amazon/sid_versions/cf_k512_dedup/
data/Amazon/sid_versions/cs_alpha0.2_k512_dedup/
data/Amazon/sid_versions/cs_alpha0.7_k512_dedup/
```

### 4.3 输出

```text
results/stage7_validation_protocol/frozen_baselines.json
results/stage7_validation_protocol/frozen_baselines.md
```

内容包括：

```json
{
  "Industrial_and_Scientific": {
    "text_exact_hr20": 0.128612,
    "cs_best_hr20": 0.1366,
    "cs_best_ndcg20": 0.0784,
    "dual_cf_exact_rerank_hr20": 0.145158,
    "dual_cf_exact_rerank_ndcg20": 0.080448
  },
  "Office_Products": {
    "text_exact_hr20": 0.144472,
    "cs_best_hr20": 0.1492,
    "cs_best_ndcg20": 0.0884,
    "dual_cf_exact_rerank_hr20": 0.172215,
    "dual_cf_exact_rerank_ndcg20": 0.101124
  }
}
```

### 4.4 验收标准

```text
1. 当前 test baseline 明确冻结。
2. 后续 test 只能做最终一次确认。
3. 新增实验的超参不能根据这些 test 结果选择。
```

---

## 5. P1：Validation Protocol 改造

### 5.1 为什么必须做

当前 Codex 指出：

```text
scripts/eval_smoke_sid_version.sh 明确从 manifest 取 test_csv。
scripts/run_dual_sid_cf_fusion.sh 也硬编码 test 路径。
evaluate.py 本身可以传任意 test_data_path。
evaluate_candidates.py 的 --test-csv 参数可以传 valid.csv。
```

所以真正的问题不是核心代码不能 valid，而是脚本层还没有 split-aware。

如果不改，所谓 validation protocol 只是文档要求，实际还是 test pipeline。

---

### 5.2 目标

让已有 evaluate / candidate / fusion / rerank 全链路都支持：

```text
EVAL_SPLIT=valid
EVAL_SPLIT=test
```

并且所有输出目录带 split 名称。

---

### 5.3 输入

SID version manifest：

```text
experiment_manifest.json
```

SID version 目录：

```text
data/Amazon/sid_versions/<sid_version>/<category>/
  train.csv
  valid.csv
  test.csv
  index.json
  item2sid.json
  sid2items.json
```

---

### 5.4 需要修改的脚本

#### 5.4.1 `scripts/eval_smoke_sid_version.sh`

新增参数：

```bash
EVAL_SPLIT=${EVAL_SPLIT:-test}
```

选择 CSV：

```text
if EVAL_SPLIT == valid:
    EVAL_CSV = manifest["valid_csv"] 或 data/Amazon/sid_versions/<v>/<c>/valid.csv

if EVAL_SPLIT == test:
    EVAL_CSV = manifest["test_csv"] 或 data/Amazon/sid_versions/<v>/<c>/test.csv
```

输出目录：

```text
results/eval_sidonly_<category>_<sid_version>_<split>_sample.../
results/calc_plus_sidonly_<category>_<sid_version>_<split>_sample.../
```

#### 5.4.2 `scripts/run_dual_sid_cf_fusion.sh`

新增参数：

```bash
EVAL_SPLIT=${EVAL_SPLIT:-test}
TEXT_CANDIDATES=...
BEHAVIOR_CANDIDATES=...
TEST_OR_VALID_CSV=...
```

输出目录：

```text
results/dual_sid_cf_k512_dedup_<category>_<split>/
```

或者：

```text
results/stage7_validation_protocol/dual_sid_cf_k512_dedup_<category>/<split>/
```

#### 5.4.3 `evaluate_candidates.py`

当前已支持 `--test-csv`，但命名应改为兼容：

```text
--eval-csv
```

保留旧参数以兼容：

```text
--test-csv 仍可用，但 help 说明这是 eval csv。
```

#### 5.4.4 `fuse_dual_sid_candidates.py`

新增或确认：

```text
--eval-csv
--split
--category
--text-candidates
--behavior-candidates
```

#### 5.4.5 `rerank.py`

新增或确认：

```text
--eval-csv
--split
--train-csv
```

其中 `train-csv` 必须用于 popularity，不能用 valid/test。

---

### 5.5 新增检查脚本

```text
scripts/check_split_alignment.py
```

输入：

```text
--text-csv data/Amazon/sid_versions/text_mbk_k512_dedup/<category>/<split>.csv
--behavior-csv data/Amazon/sid_versions/cf_k512_dedup/<category>/<split>.csv
--cs-csv optional
```

检查：

```text
row count
user_id sequence
history_item_id sequence
item_id sequence
history_item_sid nonnull
item_sid nonnull
```

输出：

```text
results/stage7_validation_protocol/<category>/<split>_alignment_report.json
```

---

### 5.6 评测指标

P1 不追求新性能，只追求协议正确：

```text
valid/test alignment pass
valid predictions generated
valid candidates generated
valid fusion report generated
valid rerank report generated
```

---

### 5.7 验收标准

```text
1. Text/CF 的 valid.csv row/user/history/item 完全对齐。
2. valid exact candidates 成功生成。
3. valid dual fusion 和 rerank 成功运行。
4. test pipeline 不再用于选择超参。
```

---

## 6. P2：Source-aware Learned Rerank

### 6.1 总原则

Codex 的修正非常重要：

```text
learned rerank 可以并行做，
但不能掩盖 Strong Behavior-SID 的贡献。
```

因此以后每个 behavior SID 的结果表必须同时包含：

```text
RRF
current heuristic rerank
learned rerank
```

否则无法判断提升来源。

---

### 6.2 目标

用 validation 上训练/选择的 learned reranker 替代固定 lambda RRF 和手工 heuristic score，缩小 union recall 到最终 top20 的排序损失。

当前 exact 模式排序损失：

```text
Industrial: union_HR@20 0.163468 -> rerank_HR@20 0.145158，gap 0.018310
Office: union_HR@20 0.189684 -> rerank_HR@20 0.172215，gap 0.017469
```

---

### 6.3 数据切分

不能 candidate-level random split。必须 row/query-level split：

```text
official valid rows
-> 70% valid_rerank_train rows
-> 30% valid_rerank_dev rows
```

一个 row 下所有 candidates 必须在同一个 split 中。

伪代码：

```python
unique_rows = sorted(valid_df["row_index"].unique())
shuffle(unique_rows, seed=42)
train_rows = first 70%
dev_rows = last 30%

features_train = features[features.row_index in train_rows]
features_dev = features[features.row_index in dev_rows]
```

---

### 6.4 输入

```text
valid dual fused candidates exact:
    dual_fused_candidates.jsonl

valid CSV:
    valid.csv

train CSV:
    train.csv，用于 popularity

embedding:
    text embedding
    cf_emb.npy
    row_index.json
    item_order.json

SID metadata:
    item2sid.json
    sid2items.json
    prefix bucket map
```

第一版先 exact，不急 p3。

---

### 6.5 Feature table

每个 candidate 一行：

```text
row_index
user_id
target_item_id
candidate_item_id
label
features...
```

label：

```text
label = 1 if candidate_item_id == target_item_id else 0
label = 0 otherwise
```

---

### 6.6 特征设计

#### A. Rank / source features

```text
text_rank
behavior_rank
min_rank
rrf_score
from_text
from_behavior
from_both
source_count
candidate_mode
```

#### B. Beam score features，可选

如果 `evaluate.py` 后续能保存 beam score：

```text
text_beam_score
behavior_beam_score
text_avg_logprob
behavior_avg_logprob
text_score_gap
behavior_score_gap
```

第一版可以没有，先基于 rank。

#### C. Popularity features

只用 train 统计：

```text
train_item_count
log_train_item_count
pop_percentile
head_mid_tail_bucket
```

#### D. History similarity features

使用 text embedding 和 behavior embedding 分别计算：

```text
max_text_cos_to_history
mean_text_cos_to_history
recent_text_cos

max_behavior_cos_to_history
mean_behavior_cos_to_history
recent_behavior_cos
```

#### E. SID / prefix quality features

```text
sid_len
has_d_token
prefix1_bucket_size
prefix2_bucket_size
prefix3_bucket_size
expanded_from_prefix
expansion_level
```

#### F. User/history features

```text
history_len
unique_history_count
history_pop_mean
history_pop_max
history_text_dispersion
history_behavior_dispersion
```

---

### 6.7 模型选择

第一版：

```text
LogisticRegression
```

优点：

```text
稳定、可解释、低风险。
```

第二版：

```text
HistGradientBoostingClassifier
GradientBoostingClassifier
LightGBM ranker，如果环境有 lightgbm
```

不要第一版直接上复杂 MLP。

---

### 6.8 输出

```text
results/stage7_5_learned_rerank/<category>/
  features_valid_exact.parquet
  valid_row_split.json
  feature_schema.json
  model_logreg.pkl
  model_gbdt.pkl
  valid_model_selection.json
  valid_metrics_summary.csv
  test_once_metrics_summary.csv
  learned_reranked_candidates.jsonl
  leakage_check_report.json
```

---

### 6.9 评测指标

必须同时报告：

```text
RRF HR@20 / NDCG@20
heuristic rerank HR@20 / NDCG@20
learned rerank HR@20 / NDCG@20
union HR@20
union - rerank gap
```

还要报告：

```text
head/mid/tail
short-history/long-history
behavior-only recovered count
from_both candidate hit rate
```

---

### 6.10 验收标准

最低标准：

```text
1. learned rerank 在 valid_rerank_dev 上超过 RRF。
2. 不出现 leakage。
3. test 只跑一次最终模型。
4. 报告中保留 RRF / heuristic / learned 三列。
```

理想标准：

```text
learned rerank 在 test 上超过当前 heuristic rerank：
Industrial > 0.145158 HR@20
Office > 0.172215 HR@20
```

如果 learned rerank 没有超过 heuristic，也不是失败：可以说明当前 heuristic 已经较强，后续重点转向 Strong Behavior-SID。

---

## 7. P3：SASRec Embedding Builder Smoke

### 7.1 为什么优先做 SASRec

当前本地只有 `sasrec.py`，没有 LightGCN。SASRec 是更现实的第一步。

但 Codex 指出，当前 `sasrec.py` 不能直接作为 Stage8 embedding builder：

```text
1. 每轮 valid 后又 eval test，违反 test final once。
2. 默认 hidden_factor=32，num_heads=1，只有一个 self-attention block。
3. seq_size=10 写死。
4. 当前保存 test logits 和 state_dict，没有导出 item embedding。
5. item embedding 和输出层 s_fc 不共享权重，直接导出 item_embeddings.weight 可能不是最适合的 item 表示。
```

因此要新增一个干净的 builder，而不是原样复用旧脚本。

---

### 7.2 目标

实现：

```text
train-only CSV
-> SASRec encoder
-> train with next-item objective
-> valid early stopping only
-> export sasrec_item_emb.npy
```

第一阶段只训练 SASRec，不训练 Qwen。

---

### 7.3 输入

```text
data/Amazon/sid_versions/text_mbk_k512_dedup/<category>/train.csv
data/Amazon/sid_versions/text_mbk_k512_dedup/<category>/valid.csv
```

字段：

```text
user_id
history_item_id
item_id
```

构造 sequence：

```text
history_item_id + item_id
```

注意：训练 SASRec 只使用 train，valid 只用于 early stopping / model selection。

---

### 7.4 item index 映射

必须使用当前 item universe，和后续 SID 对齐：

```text
item_order.json
row_index.json
```

输出：

```text
item_id -> dense_index
dense_index -> item_id
```

要求：

```text
sasrec_item_emb.npy 第 i 行对应 item_order[i]
```

---

### 7.5 推荐模型结构

第一版：

```text
embedding_dim = 128
num_layers = 2
num_heads = 2
max_seq_len = 10 或从数据自动推断
dropout = 0.2
loss = cross entropy next-item
output = h_t @ item_embedding.T
embedding tying = true
```

为什么要 tied embedding：

```text
score = h @ item_embedding.T
```

这样导出的 `item_embedding.weight[:num_items]` 明确是 next-item 目标下被训练过的 item 表示，而不是另一个独立输出层权重。

---

### 7.6 输入输出维度

输入：

```text
item_seq: [B, L]
```

输出：

```text
hidden: [B, L, d]
last_hidden: [B, d]
logits: [B, num_items]
```

导出：

```text
sasrec_item_emb.npy: [num_items, d]
```

建议：

```text
d = 128
```

不用强行转成 2560 维，因为 `run_rqkmeans_with_emb.py` 应该支持任意 embedding dimension。

---

### 7.7 Cold item 策略

SASRec 可能存在没有 train 行为的 item。

输出必须报告：

```text
behavior_covered_items
cold_items
cold_rate
zero_or_random_init_items
cold_fallback_policy
```

第一版建议：

```text
不 fallback，保留随机/低质量 embedding，并明确报告 cold_rate。
```

如果使用 text fallback，版本名必须写清楚：

```text
sasrec_textfallback_k512_dedup
```

避免和纯 SASRec-SID 混淆。

---

### 7.8 输出

```text
data/Amazon/behavior_embeddings/sasrec/<category>/
  sasrec_item_emb.npy
  item_order.json
  row_index.json
  sasrec_config.json
  train_report.json
  valid_metrics.json
  embedding_health_report.json
  nearest_neighbor_examples.json
```

---

### 7.9 评测指标

传统推荐指标：

```text
SASRec valid HR@10/20/50
SASRec valid NDCG@10/20/50
```

embedding 健康指标：

```text
shape
dtype
zero_rows
norm_mean
norm_min
norm_max
coverage
cold_rate
nearest-neighbor sanity
```

---

### 7.10 验收标准

进入 P4 前必须满足：

```text
1. sasrec_item_emb.npy 成功导出。
2. 行数等于 num_items。
3. row_index / item_order 与 SID item universe 对齐。
4. zero rows 或 cold rows 有报告。
5. SASRec valid HR/NDCG 明显不是随机。
```

---

## 8. P4：SASRec-SID Quality + 小规模 SFT + Dual Fusion

### 8.1 目标

验证：

```text
SASRec item embedding 是否比 PPMI/SVD cf_emb.npy 更适合作为 Behavior-SID 输入。
```

---

### 8.2 输入

```text
data/Amazon/behavior_embeddings/sasrec/<category>/sasrec_item_emb.npy
data/Amazon/behavior_embeddings/sasrec/<category>/row_index.json
data/Amazon/behavior_embeddings/sasrec/<category>/item_order.json
```

---

### 8.3 生成 SASRec-SID

复用：

```text
run_rqkmeans_with_emb.py
rewrite_sid_csv.py
update_sid_manifest.py
check_sid_stage0.py
analyze_sid.py
```

生成：

```text
data/Amazon/sid_versions/sasrec_k512_dedup/<category>/
```

参数：

```text
codebook_size = 512
levels = 3
dedup_mode = append
token format = <a_*><b_*><c_*><d_*>
```

不要第一版就改 K 或层数，否则变量太多。

---

### 8.4 SID 质量评测

对比：

```text
cf_k512_dedup
sasrec_k512_dedup
```

指标：

```text
num_items
unique_sid
pre_dedup_collision_rate
post_dedup_collision_rate
sid_len_dist
d_token_ratio
prefix1/2/3 fanout
max_prefix_bucket
p95_prefix_bucket
token utilization
```

注意：

```text
不要把 d_token_ratio 作为唯一验收标准。
最终仍然看 behavior HR、behavior_only_hit、union HR、rerank HR/NDCG。
```

---

### 8.5 小规模 SFT

先做 10k smoke：

```text
sample=10000
epoch=1
beam=20
candidate_mode=exact
```

通过后再做 30k：

```text
sample=30000
epoch=1
beam=20
candidate_mode=exact
```

不要一开始 p3。

---

### 8.6 SFT 输入输出

输入 CSV：

```text
data/Amazon/sid_versions/sasrec_k512_dedup/<category>/train.csv
```

字段：

```text
history_item_sid
item_sid
```

模型：

```text
Qwen2.5-0.5B decoder-only causal LM
```

输入：

```text
history SASRec-SID sequence
```

输出：

```text
target SASRec-SID
```

---

### 8.7 Candidate / Fusion / Rerank

生成：

```text
sasrec exact candidates
```

融合：

```text
Text-SID exact candidates + SASRec-SID exact candidates
```

必须报告三列：

```text
RRF
current heuristic rerank
learned rerank
```

---

### 8.8 输出

```text
results/stage8_sasrec_behavior_sid/<category>/
  sid_quality/
  sft_10k/
  sft_30k/
  candidates_exact/
  dual_fusion_exact/
  heuristic_rerank/
  learned_rerank/
  metrics_summary.csv
  metrics_summary.md
```

---

### 8.9 验收标准

最低标准：

```text
1. SASRec-SID post-dedup collision = 0。
2. CSV rewrite missing = 0。
3. check_sid_stage0 pass。
4. 10k SFT/eval 可跑通。
5. exact candidates 可生成。
```

理想标准：

```text
1. sasrec_k512_dedup 的 d-token ratio 低于 cf_k512_dedup。
2. behavior-only HR@20 高于 cf_k512_dedup。
3. behavior_only_hit20 高于 cf_k512_dedup。
4. Text + SASRec exact dual rerank 高于 Text + CF exact dual rerank。
```

---

## 9. P5：LightGCN Behavior-SID

### 9.1 当前可行性

Codex 判断正确：

```text
本地没有 LightGCN 实现。
不能说复用现有 LightGCN。
需要新增训练脚本。
```

因此 LightGCN 是 P4 之后的第二优先级。

---

### 9.2 目标

实现：

```text
train-only user-item graph
-> LightGCN
-> lightgcn_item_emb.npy
-> lightgcn_k512_dedup
-> SFT
-> Text + LightGCN dual fusion
```

---

### 9.3 图构建

Amazon train CSV 是 prefix-style sequence 行，如果每行都把 `history + target` 全量加边，会重复统计同一个 user-item 很多次。

第一版建议：

```text
按 user_id 聚合 train positives
每个 user-item 去重建边
```

同时保存 count 作为可选权重：

```text
edge_weight = count(user,item)
```

第一版 LightGCN 可以用 unweighted edges，报告 count 分布。

---

### 9.4 输入

```text
train.csv:
  user_id
  history_item_id
  item_id
```

构造：

```text
user_positive_items[user].add(each item in history)
user_positive_items[user].add(target)
```

---

### 9.5 模型结构

第一版：

```text
embedding_dim = 128
num_layers = 2 or 3
loss = BPR
negative_sampling = uniform
epochs = 100-200
early stopping = valid HR@20
```

---

### 9.6 embedding 导出定义

LightGCN 推荐导出：

```text
layer-averaged item embedding
```

也就是：

```text
item_emb_final = mean(item_emb_layer0, item_emb_layer1, ..., item_emb_layerK)
```

输出：

```text
lightgcn_item_emb.npy: [num_items, 128]
```

---

### 9.7 输出

```text
data/Amazon/behavior_embeddings/lightgcn/<category>/
  lightgcn_item_emb.npy
  lightgcn_user_emb.npy
  item_order.json
  row_index.json
  user_index.json
  graph_stats.json
  lightgcn_config.json
  train_report.json
  valid_metrics.json
```

后续完全复用 P4 的 SID/SFT/fusion/rerank 流程。

---

### 9.8 评测指标

```text
LightGCN valid HR@20/NDCG@20
embedding coverage
cold item count
d-token ratio
behavior-only HR@20
behavior_only_hit20
union HR@20
RRF / heuristic / learned rerank HR/NDCG
```

---

## 10. P6：Gated p3 / Noise-aware Prefix Expansion

### 10.1 为什么延后做

当前 dual-stream 下 p3 噪声偏大，exact 更强：

```text
Industrial exact + rerank HR@20 = 0.145158
Industrial p3 + rerank HR@20 = 0.141407

Office exact + rerank HR@20 = 0.172215
Office p3 + rerank HR@20 = 0.168722
```

所以 p3 不应该作为下一阶段第一优先级。应该等 exact + learned rerank / strong behavior SID 稳定后再做。

---

### 10.2 目标

把粗暴 p3：

```text
prefix@3 -> 展开所有 item
```

改成受控 p3：

```text
prefix@3 -> gated / top-M / agreement-based expansion
```

---

### 10.3 方法

#### A. Bucket-threshold gated p3

```text
if prefix3_bucket_size <= T:
    expand
else:
    skip or keep exact only
```

候选：

```text
T ∈ {5, 10, 20, 50}
```

#### B. Top-M p3

```text
prefix bucket items
-> 按 history cosine / popularity / source agreement 排序
-> 只保留 top M
```

候选：

```text
M ∈ {5, 10, 20}
```

#### C. Dual-agreement p3

```text
如果 p3 item 也被另一 stream exact/p3 支持，则加权。
如果 item 只来自巨大 prefix bucket，则降权。
```

#### D. p3-as-feature

不直接扩候选池，而是给 exact candidates 加特征：

```text
same_prefix3_as_generated_sid
prefix3_bucket_size
prefix3_hit_count
```

---

### 10.4 调参协议

所有 threshold / M / agreement weight：

```text
只在 valid 上选择。
```

test 只跑最终一次。

---

### 10.5 评测指标

```text
mean_candidates
median_candidates
p95_candidates
candidate_recall@20/50/500
HR@20
NDCG@20
union-rerank gap
prefix bucket fanout
noise ratio
```

---

### 10.6 验收标准

最低标准：

```text
gated p3 的 mean_candidates 明显低于 raw p3。
HR/NDCG 不低于 raw p3。
```

理想标准：

```text
gated p3 + learned rerank >= exact + learned rerank。
```

如果达不到，最终报告仍以 exact 为主，p3 作为消融。

---

## 11. P7：ActionPiece-light / Learnable Tokenizer，可选研究增强

### 11.1 为什么不是当前第一优先级

ActionPiece-light 和 learnable tokenizer 都是研究价值高但工程复杂的方向。当前项目最需要先解决：

```text
valid protocol
SASRec exporter
learned rerank
Strong Behavior-SID
```

因此 ActionPiece-light 放到后续。

---

### 11.2 ActionPiece-light 目标

不改变 target SID，只增强 history input：

```text
原输入:
SID(item_1), SID(item_2), SID(item_3)

增强输入:
SID(item_1), <ctx_*>, SID(item_2), <ctx_*>, SID(item_3)

target:
SID(target_item)
```

这样不破坏 item2sid 契约。

---

### 11.3 输入 feature set

每个 item：

```text
A_i = {
  text SID tokens,
  behavior SID tokens,
  category token,
  popularity bucket,
  optional audience cluster
}
```

---

### 11.4 新增脚本

```text
build_actionpiece_light_vocab.py
rewrite_actionpiece_light_csv.py
```

还需要修改：

```text
sft.py:
    支持 --extra-token-file

utils_sid.py:
    SID parser 不把 <ctx_*> 当 target SID token

data.py:
    输入侧允许 <ctx_*>
    target 侧仍只监督合法 SID
```

---

### 11.5 评测指标

```text
HR@20/NDCG@20
invalid_sid_rate
context_token_utilization
avg input length
p95 input length
short-history vs long-history
w/o ctx ablation
within-item ctx vs adjacent ctx
```

---

## 12. 总体实验矩阵

### 12.1 当前 frozen baselines

| Method | SID | Fusion/Rerank | 作用 |
|---|---|---|---|
| Text-SID | text_mbk_k512_dedup | none | semantic baseline |
| CS-SID | cs_alpha best | heuristic rerank | strongest single-SID baseline |
| CF-SID | cf_k512_dedup | none | current behavior baseline |
| Dual-CF | text + cf | exact + heuristic rerank | current strongest item-level baseline |

### 12.2 新增实验

| 阶段 | 方法 | 是否训练 Qwen | 是否训练行为模型 | 目的 |
|---|---|---:|---:|---|
| P1 | valid protocol | 否 | 否 | 修复实验协议 |
| P2 | learned rerank | 否 | 否 | 缩小 sorting loss |
| P3 | SASRec exporter | 否 | 是 | 产出强序列 behavior embedding |
| P4 | SASRec-SID | 是 | 已有 | 验证强行为 SID |
| P5 | LightGCN-SID | 是 | 是 | 验证高阶图协同 |
| P6 | gated p3 | 否 | 否 | 降低 prefix 噪声 |
| P7 | ActionPiece-light | 是 | 可选 | 上下文 tokenization |

---

## 13. 关键结果表模板

### 13.1 SID quality

| Category | Behavior Embedding | SID Version | d-token ratio | pre-dedup collision | post-dedup collision | prefix3 p95 fanout |
|---|---|---|---:|---:|---:|---:|

### 13.2 Behavior-only

| Category | Behavior SID | HR@20 | NDCG@20 | invalid rate | behavior-only hits@20 |
|---|---|---:|---:|---:|---:|

### 13.3 Dual fusion

| Category | Behavior SID | Fusion | HR@20 | NDCG@20 | Union HR@20 | Union-rerank gap |
|---|---|---|---:|---:|---:|---:|
| Industrial | CF | RRF |  |  |  |  |
| Industrial | CF | heuristic rerank |  |  |  |  |
| Industrial | CF | learned rerank |  |  |  |  |
| Industrial | SASRec | RRF |  |  |  |  |
| Industrial | SASRec | heuristic rerank |  |  |  |  |
| Industrial | SASRec | learned rerank |  |  |  |  |

---

## 14. 阶段停止条件

### P1 停止条件

```text
如果 valid split pipeline 不能完整跑通，不进入 learned rerank 或 SASRec-SID。
```

### P2 停止条件

```text
如果 learned rerank 在 valid_rerank_dev 上不超过 heuristic，可以暂缓 learned rerank；
但仍保留作为对照，不让它参与 Strong Behavior-SID 主要结论。
```

### P3 停止条件

```text
如果 SASRec embedding 无法覆盖 item universe，或导出映射不可靠，不进入 SID 构造。
```

### P4 停止条件

```text
如果 SASRec-SID quality 明显差于 CF-SID，且 10k SFT 无 behavior-only 补充，不继续 30k。
```

### P5 停止条件

```text
如果 LightGCN-SID 不能提升 behavior-only hits 或 union recall，不继续 p3/ActionPiece。
```

---

## 15. 最终给 Codex 的实现 Prompt

```text
你现在基于 MiniOneRec 当前 Dual-SID Text+CF 结果继续做下一阶段优化。请严格按下面顺序推进，不要直接跳到 LightGCN 或 ActionPiece。

总体原则：
1. 不要再用 test set 调参。
2. 所有超参选择必须基于 validation。
3. learned rerank 必须按 row/query split，不能按 candidate 行随机 split。
4. 每个 behavior SID 都必须同时报告 RRF、current heuristic rerank、learned rerank，避免把 Strong Behavior-SID 的贡献和 reranker 贡献混在一起。
5. 不要删除现有 results。

当前 frozen baseline：
- Industrial Stage 6 CS best: 0.1366 / 0.0784
- Office Stage 6 CS best: 0.1492 / 0.0884
- Industrial Dual-CF exact + rerank: 0.145158 / 0.080448
- Office Dual-CF exact + rerank: 0.172215 / 0.101124

P0: Baseline archive
- 生成 results/stage7_validation_protocol/frozen_baselines.json/md。
- 记录当前 Text、CS、CF、Dual-CF baseline。
- 明确 test 后续只用于 final once。

P1: Validation protocol
- 修改 scripts/eval_smoke_sid_version.sh 支持 EVAL_SPLIT=valid/test。
- 修改 scripts/run_dual_sid_cf_fusion.sh 支持 EVAL_SPLIT=valid/test。
- evaluate_candidates.py 的 --test-csv 改为兼容 --eval-csv。
- 所有输出目录带 split 名称。
- 新增 scripts/check_split_alignment.py，检查 Text/CF/CS 的 row_count、user_id、history_item_id、item_id。
- 先跑 valid exact candidates/fusion/rerank。

P2: Source-aware learned rerank
- 从 valid dual fused exact candidates 构造 candidate-level feature table。
- official valid rows 按 row_index 切成 70% valid_rerank_train、30% valid_rerank_dev。
- 不允许 candidate 行随机切分。
- 特征包括：
  text_rank, behavior_rank, min_rank, rrf_score,
  from_text, from_behavior, from_both,
  train-only popularity,
  history_len,
  text history cosine,
  behavior history cosine,
  recent cosine,
  sid_len, has_d_token, prefix bucket size。
- label = candidate_item_id == target_item_id。
- 第一版训练 LogisticRegression，第二版可试 sklearn GBDT/LightGBM。
- 在 valid_rerank_dev 上选择模型。
- test 只跑最终一次。
- 必须输出 RRF / heuristic rerank / learned rerank 三列。

P3: SASRec embedding builder smoke
- 不要原样复用旧 sasrec.py。
- 新增 train_sasrec_item_embedding.py。
- 只用 train CSV 训练，valid early stopping，不 eval test。
- 构造 dense item index，并与 item_order.json / row_index.json 对齐。
- 模型第一版：
  embedding_dim=128
  num_layers=2
  num_heads=2
  max_seq_len 从数据推断或配置
  dropout=0.2
  score = h @ item_embedding.T
  tied embedding
  loss = CE next-item
- 导出：
  sasrec_item_emb.npy
  item_order.json
  row_index.json
  sasrec_config.json
  train_report.json
  valid_metrics.json
  embedding_health_report.json
- 报告 coverage、cold_items、cold_rate、zero_rows、norm 分布、nearest neighbors。

P4: SASRec-SID quality and SFT
- 用 run_rqkmeans_with_emb.py 生成 sasrec_k512_dedup。
- 参数保持 codebook_size=512, levels=3, dedup_mode=append。
- rewrite train/valid/test CSV。
- 运行 check_sid_stage0.py 和 analyze_sid.py。
- 对比 cf_k512_dedup:
  d-token ratio,
  pre/post collision,
  prefix fanout,
  token utilization。
- 先跑 10k sid-only SFT smoke，candidate_mode=exact。
- 通过后再跑 30k。
- Text + SASRec exact dual fusion。
- 同时报 RRF / heuristic rerank / learned rerank。
- 对比 CF-SID:
  behavior-only HR,
  behavior_only_hit20,
  union HR,
  rerank HR/NDCG。

P5: LightGCN-SID
- 当前本地没有 LightGCN，需要新增 train_lightgcn_item_embedding.py。
- train CSV 按 user_id 聚合 positives。
- 对每个 user-item 去重建边，不要直接对每行 history+target 重复加边。
- 可另存 count/weight 作为附加统计。
- 训练 LightGCN:
  embedding_dim=128
  num_layers=2 or 3
  BPR loss
  valid early stopping
- 导出 layer-averaged item embedding。
- 后续同 SASRec-SID 流程。

P6: Gated p3
- 在 exact + learned rerank 稳定后再做。
- 实现：
  bucket-threshold p3: T in {5,10,20,50}
  top-M p3: M in {5,10,20}
  dual-agreement p3
  p3-as-feature
- 所有阈值只在 valid 上选。
- test 只跑 best valid setting once。

P7: ActionPiece-light, optional
- 不改变 target SID。
- 只在 history input 侧加入 <ctx_*>。
- 新增 build_actionpiece_light_vocab.py 和 rewrite_actionpiece_light_csv.py。
- sft.py 支持 --extra-token-file。
- utils_sid.py 不把 <ctx_*> 当作目标 SID token。
- 做 w/o ctx、within ctx、adjacent ctx 消融。
```

---

## 16. 最终结论

最终版方案的核心是：

```text
先协议，后模型；
先 exact，后 p3；
先 SASRec，后 LightGCN；
先固定对照，再 learned rerank；
先 Strong Behavior-SID，再 ActionPiece-light。
```

这样做的好处是：

```text
1. 实验结论干净，不再 test tuning。
2. Strong Behavior-SID 的贡献不会被 learned rerank 掩盖。
3. SASRec embedding builder 成为后续所有 behavior-SID 的标准接口。
4. 当前已有 SID/SFT/fusion/rerank 后半段可以最大化复用。
5. 每一步都有明确输入、输出、指标和停止条件。
```
